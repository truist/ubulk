# hard-coded defaults; don't edit this file. see ubulk.conf instead

# the syntax here is designed to very carefully avoid trouncing any
# values passed as command-line arguments

: ${CONFIG:=/etc/ubulk.conf}

: ${PKGSRC:=/usr/pkgsrc}
: ${BUILDLOG:=/var/log/ubulk-build.log}

